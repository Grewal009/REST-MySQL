-- select database
use insta_app;

-- select all records from user table
SELECT * FROM user;

-- use vscode terminal CLI for mysql instead of mysql workbench
-- enter following command
--  /usr/local/mysql/bin/mysql -u root -p 
